version: "{build}"

image: Visual Studio 2019 Preview

cache: 
  - c:\tools\vcpkg\installed\
      
install:
  - cd %APPVEYOR_BUILD_FOLDER%
  - git submodule update --init --recursive
  - cd c:\tools\vcpkg
  - git pull
  - vcpkg update
  - vcpkg integrate install
  - vcpkg install nlohmann-json:x86-windows-static nlohmann-json:x64-windows-static
  - vcpkg install boost-property-tree:x86-windows-static boost-property-tree:x64-windows-static
  - vcpkg install poco:x86-windows-static poco:x64-windows-static
  - cd %APPVEYOR_BUILD_FOLDER%
      
before_build:
  - mkdir build
  - mkdir vcproj
  - mkdir vcproj64
  - cd vcproj
  - cmake .. -G "Visual Studio 15 2017" -DVCPKG_TARGET_TRIPLET=x86-windows-static
  - cd ..
  - cd vcproj64
  - cmake .. -G "Visual Studio 15 2017 Win64" -DUSE_64BIT_BUILD=ON -DVCPKG_TARGET_TRIPLET=x64-windows-static -DCMAKE_TOOLCHAIN_FILE=c:/tools/vcpkg/scripts/buildsystems/vcpkg.cmake -DVCPKG_TARGET_TRIPLET=x64-windows-static
  - cd ..

build:
  parallel: true
  verbosity: minimal

build_script:
  - msbuild vcproj\template-plugin.sln /m 
  - msbuild vcproj64\template-plugin.sln /m 
  - tools\hemtt.exe build
  
after_build:
  - ps: .\tools\packageAppveyor.ps1

artifacts:
- path: build\win32
  name: win32
- path: build\win64
  name: win64
- path: addons\grad_replay_intercept.pbo
  name: grad_replay_intercept_pbo



for:
-
  branches:
    only:
      - master
  
  deploy:
    - provider: GitHub
      auth_token:
        secure: MXV97Ngzyn/C/TrCbWG4tdIZcxS/rTfojUq45KbgspSafhcTut3HseMq2002oYkg
      artifact: zip
      draft: false
      prerelease: false
      force_update: true
      skip_tags: false
      on:
        APPVEYOR_REPO_TAG: true

  configuration: Release
  
-
  configuration: Debug